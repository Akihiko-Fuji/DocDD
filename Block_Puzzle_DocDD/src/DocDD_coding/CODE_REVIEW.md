# DocDD_coding コードレビュー（2026-05-12）

対象: `Block_Puzzle_DocDD/src/DocDD_coding/`

## 総評
- 実装はモジュール分離（ルール系と描画系の分離）ができており、`tests/DocDD_coding` も17件すべて成功しているため、A-TYPE最小実装として基礎品質は満たしています。
- 一方で、DocDD方針との整合性・文書追従の観点で、修正推奨の不整合が2点あります（READMEの説明揺れ、テスト配置の一貫性）。

## 実行確認
- `python -m pytest tests/DocDD_coding -q`
  - 結果: 17 passed

## 良い点
1. **状態遷移が簡潔で可読性が高い**
   - `TITLE/SETUP_A/PLAY/PAUSE/GAMEOVER` の遷移が `StateController` に明確に集約され、仕様追跡しやすい。
2. **入力写像が分離されている**
   - `input_mapper.py` に論理入力変換が分離され、将来の `config.ini` 拡張余地がある。
3. **テストが主要仕様を広くカバー**
   - ピース形状、回転衝突、盤面処理、スコア/レベル、T-Spin、状態遷移、非採用機能まで網羅されている。

## 指摘事項（推奨）

### 1) READMEの実装説明が初期方針と読み取り上ズレる
- 現在のREADMEには、画像・フォントアセットを「優先利用」とある一方、初期方針は「アセット未整備でも成立（矩形描画・標準フォント中心）」を強く要求しているため、読者に「アセット前提実装」と誤認される余地があります。
- 実際のコードがフォールバックを持つ点は良いですが、DocDD教材としてはREADMEの表現を「アセット非依存で成立、アセットは任意利用」に寄せたほうが上流文書とのトレーサビリティが明確です。

### 2) テスト配置の一貫性
- `test_tspin_lock_integration.py` が `src/DocDD_coding/` 配下にあり、他のテストが `tests/DocDD_coding/` にある構成と分離しています。
- 実害は小さいものの、QA文書と実装の対応説明時に「どこまでが正式テストスイートか」が曖昧になりやすいです。
- `tests/DocDD_coding/` に集約し、必要なら `41/42/43` との対応IDコメントを付けるのが望ましいです。

## 追加確認推奨
- READMEの「外部著作物を同梱しない」という記述と、`art/` 利用方針の関係を明確化（同梱物の出所ルール明記）。
- 仕様差分の判断（例: 24aの座標差異）は、READMEだけでなく `62_review_log.md` など記録層にも残すと、将来レビュー時の再検証コストを下げられます。

## 結論
- 現状は「実装としては成立、DocDD教材としては説明文の精密化余地あり」という評価です。
- 次アクションとしては、README表現整理とテスト配置統一を優先するのが妥当です。
